@inject NavigationManager NavManager

<nav class="navbar navbar-expand-md navbar-dark bg-transparent custom-sticky-navbar">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <div class="navbar-brand page-title d-flex align-items-center">
            SCOOPY
        </div>

        <div class="collapse navbar-collapse d-flex justify-content-end" id="mainNavbar">
            <div class="nav-links-container">
                <NavLink class="nav-link-item" href="/" match="NavLinkMatch.All">Home</NavLink>
                <NavLink class="nav-link-item" href="/menu">Shop</NavLink>
                <NavLink class="nav-link-item" href="/about">About Us</NavLink>
                <NavLink class="nav-link-item" href="/contact">Contact </NavLink>
            </div>
        </div>
    </div>
</nav>

<style>
    .navbar {
        padding: 20px 0;
        background-color: transparent !important; /* Make navbar transparent */
        box-shadow: none; /* Remove shadow */
        position: absolute; /* Position over hero */
        width: 100%;
        top: 0;
    }

    .navbar-brand {
        font-weight: 700;
        font-size: 24px;
        color: white !important; /* Change to white text */
        text-decoration: none;
    }

    .nav-links-container {
        display: flex;
        gap: 30px;
    }

    .nav-link-item {
        color: white !important; /* Change to white text */
        font-weight: 600;
        text-decoration: none;
        text-transform: uppercase;
        font-size: 14px;
        letter-spacing: 1px;
        transition: color 0.3s ease;
    }

        .nav-link-item:hover {
            color: #ff6b9d !important;
        }

        .nav-link-item.active {
            color: #ff6b9d !important;
            font-weight: 700;
        }
</style>
